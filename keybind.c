#include "cub3d.h"

int	ft_keybind(int keysym, t_game *game)
{
	if (keysym == KEY_W)
		;
	if (keysym == KEY_A)
		;
	if (keysym == KEY_D)
		;
	if (keysym == KEY_S)
		;
	if (keysym == KEY_LEFT)
		;
	if (keysym == KEY_RIGHT)
		;
	if (keysym == KEY_ESC)
		ft_closegame(game);
	return (0);
}
